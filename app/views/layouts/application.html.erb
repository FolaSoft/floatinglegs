<!DOCTYPE html>
<html>
<head>
  <title> <%= full_title(yield(:title)) %></title>
  <%= render 'layouts/stylesheet_link_tag' %>
  <%= render 'layouts/javascript_include_tags' %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>
</head>
<body>
  <%= render 'layouts/navbar' %>
   <div class="container">
     <div class="jumbotron">
      <%= yield %>
    </div>
   </div>
 </body>
</html>
